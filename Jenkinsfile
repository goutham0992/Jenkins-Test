pipeline {
      agent any
      
      stages {
            stage('Build') {
            steps { 
                  sh 'ping www.google.com'
                  }
                  
                  }
                  }
                  }
